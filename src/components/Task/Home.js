import {useContext, useState} from "react";
import {Card, Button, Container, ToastContainer, Toast} from "react-bootstrap";
import UserContext from "../../store/user-context";

const Home = () => {
    return (
        <h1>HOME COMPONENT</h1>
    );
};

export default Home;
